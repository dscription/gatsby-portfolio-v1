import React from "react"
import styled from "styled-components"

export const SubHeading = styled.h2`
  font-size: ${props => props.theme.fonts.primary.sub};
  line-height: ${props => props.theme.fonts.primary.subHeight};
`
