import React from "react"
import styled from "styled-components"

export const SubHeading = styled.h2`
  font-size: ${props => props.theme.fonts.primary.small.sub};
  line-height: ${props => props.theme.fonts.primary.small.subHeight};
  margin: 10px 0px;
`
